# GitHub storage
GITHUB_TOKEN=ghp_...
GITHUB_REPO=yourorg/vineland-results
GITHUB_BRANCH=main

# Paths (relative to working directory or absolute)
ITEMS_YAML=items/items.yaml
AGENTS_YAML=configs/agents.yaml
PILOT_YAML=configs/pilot.yaml

# Judge agent (must be reachable from the server)
JUDGE_AGENT_ID=claude-opus-4-5
JUDGE_API_KEY_ENV=OPENROUTER_API_KEY
OPENROUTER_API_KEY=sk-or-...

# Run settings
N_REPLICATIONS=5
MAX_CONCURRENCY=2
RUNS_DIR=runs/api
